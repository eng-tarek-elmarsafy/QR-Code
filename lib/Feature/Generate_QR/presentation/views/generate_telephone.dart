import 'package:flutter/material.dart';

class GenerateTelephone extends StatelessWidget {
  static const String id = '/GenerateTelephone';
  const GenerateTelephone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}