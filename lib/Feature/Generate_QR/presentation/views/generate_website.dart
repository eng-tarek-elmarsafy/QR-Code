import 'package:flutter/material.dart';

class GenerateWebsite extends StatelessWidget {
   static const String id = '/GenerateWebsite';
  const GenerateWebsite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}