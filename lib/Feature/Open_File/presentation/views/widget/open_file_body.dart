import 'package:flutter/material.dart';

class OpenFileBody extends StatelessWidget {
  const OpenFileBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}